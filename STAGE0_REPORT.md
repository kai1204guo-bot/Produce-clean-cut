# 阶段 0 技术验证记录

## 当前结论

媒体预检、文本软字幕处理、硬字幕分析、遮罩渲染和逐帧画面修复链路已经验证可行。

## 已验证环境

- Windows；
- Python 3.11.15；
- FFmpeg/ffprobe 8.1.1；
- 使用标准库实现核心逻辑，当前没有第三方 Python 运行依赖。
- 硬字幕OCR可选依赖为RapidOCR 3.x与ONNX Runtime。
- 视频修复依赖 OpenCV 4.10+；LaMa 可使用 ONNX Runtime CPU 或 CUDA。
- NVIDIA GeForce RTX 5060 Ti 上已验证 FP32 LaMa 的 CUDA 执行提供程序。
- 官方 STTN 权重已在 PyTorch 2.11 CPU 与 CUDA 12.8 环境完成真实推理验证。

## 已完成验证

1. 读取视频容器、时长、轨道、编码、语言、默认字幕和时间基；
2. 区分文本软字幕、图像软字幕和可能的硬字幕；
3. 自动选择默认文本字幕轨道，没有默认轨道时选择第一条；
4. 将文本字幕转换为 UTF-8 SRT；
5. 不重新编码视频和音频，重新封装出无字幕文件；
6. 生成 JSON 任务报告；
7. 临时输出成功后再原子替换到正式文件；
8. 正式输出已存在时拒绝覆盖；
9. 合成带中文 SRT、H.264 视频和 AAC 音频的 MKV 测试文件，完成端到端验证；
10. 验证处理后视频保留音频且不存在字幕轨道。
11. 定义OCR文字、多边形、字幕区域、字幕条目和遮罩关键帧协议；
12. 接入RapidOCR，并验证英文、数字和中文合成字幕识别；
13. 实现多行字幕合并、OCR抖动投票、位置匹配和字幕变化拆分；
14. 从视频抽帧，经OCR轨迹同时生成SRT与逐帧遮罩计划；
15. 验证中文目录下的开发环境运行方式。
16. 实现多边形遮罩扩张、羽化和字幕淡入淡出时间保持；
17. 实现 OpenCV Telea 与 LaMa ONNX 两个可切换修复后端；
18. 验证 CFR 视频逐帧修复、FFV1 无损中间文件、H.264 单次编码和原音频回封；
19. 增加模型下载、固定 SHA-256 校验和模型文件 Git 排除；
20. 对可疑 VFR 输入拒绝修复，避免按帧号计算时间造成漂移；
21. 修复 Windows 下 NVIDIA Python wheel 的 CUDA/cuDNN 动态库发现问题。
22. 将官方 STTN 推理网络适配到 Python 3.11 与现代 PyTorch，并保持权重键严格匹配；
23. 实现 30 帧时序分块和 5 帧上下文重叠，避免一次载入整条长视频；
24. 实现结构、音频、时长、遮罩 MAE/PSNR 和时序残差质量报告；
25. 官方 Google Drive 与下载镜像中的 STTN 权重哈希交叉验证一致；
26. RTX 5060 Ti 上三帧实模冒烟测试约 2.1 秒，CPU 约 7.4 秒。
27. 实现灰度直方图与像素变化组合的硬切检测，并在切镜处重置 STTN 上下文；
28. 实现独立场景检测命令，输出切镜帧号、时间戳和置信分数；
29. 仅在原字幕活跃时段执行输出 OCR，并以文字相似度过滤无关背景文字；
30. 将疑似残留合并为可定位时间区间，并区分 `passed`、`needs_review`、`not_applicable` 和 `inconclusive`，避免无样本时误判通过。

## 自动化测试

当前包含 26 项测试（未配置模型时 LaMa/STTN 实模测试自动跳过）：

- 文本与图像字幕编码分类；
- 文本软字幕分流；
- 无字幕轨道分流；
- 文本字幕提取与清水视频重新封装的端到端测试。
- OCR区域裁剪与坐标回映；
- 英文、数字和中文合成字幕识别；
- 多行字幕合并、字幕切换和位置隔离；
- 遮罩关键帧分组；
- 视频抽帧到SRT及遮罩计划的端到端测试。
- 遮罩时间保持、扩张与羽化；
- OpenCV 静态画面修复；
- 视频修复与音频保留端到端测试；
- CFR/VFR 帧率判定；
- 可选的 LaMa CPU/CUDA 实模推理测试。
- 时序后端重叠分块与输出帧数；
- 有/无干净参考视频的质量指标协议；
- 可选的 STTN CPU/CUDA 实模推理测试。
- 硬切检测、静态画面抗误报与切镜时间戳；
- STTN 在切镜处刷新时序块的端到端验证；
- 相似字幕残留命中和无关背景文字过滤。

GitHub Actions 会在每次推送和拉取请求时使用 Python 3.11 与 FFmpeg 重跑测试。

## 尚未覆盖

- PGS/VobSub 等图像字幕 OCR；
- VFR 的逐帧 PTS 修复链路；
- LaMa 的分块/批处理与长视频性能优化；
- 渐变、闪光和快速摇镜等复杂转场分类；
- STTN 自适应显存分块；
- PySide6 桌面界面。

## 下一验证批次

下一批优先建立质量评测与时序修复基准：

1. 建立带描边、双行、渐隐和中英混排字幕的合成测试素材；
2. 记录 OpenCV、LaMa、STTN 的长片吞吐、显存和时序残差数据；
3. 引入逐帧 PTS，支持 VFR 视频；
4. 将任务状态、断点和失败信息持久化到 SQLite；
5. 建立首批真实授权片段的人工复核基准。
