# 阶段 0 技术验证记录

## 当前结论

媒体预检和文本软字幕处理链路已经验证可行，可以作为后续硬字幕处理的统一入口。

## 已验证环境

- Windows；
- Python 3.11.15；
- FFmpeg/ffprobe 8.1.1；
- 使用标准库实现核心逻辑，当前没有第三方 Python 运行依赖。

## 已完成验证

1. 读取视频容器、时长、轨道、编码、语言、默认字幕和时间基；
2. 区分文本软字幕、图像软字幕和可能的硬字幕；
3. 自动选择默认文本字幕轨道，没有默认轨道时选择第一条；
4. 将文本字幕转换为 UTF-8 SRT；
5. 不重新编码视频和音频，重新封装出无字幕文件；
6. 生成 JSON 任务报告；
7. 临时输出成功后再原子替换到正式文件；
8. 正式输出已存在时拒绝覆盖；
9. 合成带中文 SRT、H.264 视频和 AAC 音频的 MKV 测试文件，完成端到端验证；
10. 验证处理后视频保留音频且不存在字幕轨道。

## 自动化测试

当前包含 4 项测试：

- 文本与图像字幕编码分类；
- 文本软字幕分流；
- 无字幕轨道分流；
- 文本字幕提取与清水视频重新封装的端到端测试。

GitHub Actions 会在每次推送和拉取请求时使用 Python 3.11 与 FFmpeg 重跑测试。

## 尚未覆盖

- PGS/VobSub 等图像字幕 OCR；
- 烧录硬字幕检测和 OCR；
- OCR 结果跨帧合并；
- STTN、LaMa ONNX 修复；
- VFR 逐帧时间戳验证；
- PySide6 桌面界面。

## 下一验证批次

下一批优先建立硬字幕中间数据协议和样本基准：

1. 定义文字框、OCR 候选、字幕轨迹和逐帧遮罩的数据结构；
2. 接入一种可替换的 OCR 后端；
3. 对固定字幕区域执行抽帧、检测、识别和轨迹合并；
4. 从同一字幕轨迹生成 SRT 与遮罩；
5. 建立带描边、双行、渐隐和中英混排字幕的合成测试素材。

