# 自动化清水版剪辑软件：第一版技术方案

## 1. 产品目标

输入一个视频，自动判断字幕类型，并输出：

1. 不带字幕的清水视频；
2. 与原视频字幕内容和时间轴对应的 SRT 文件；
3. 可恢复、可复核的处理工程数据；
4. 可选的处理质量报告。

首版是 Windows 10/11 本地桌面软件。所有视频、音频、截图、OCR 文字和模型推理默认在本机完成。

## 2. 第一版功能边界

### 2.1 必须实现

- 支持 MP4、MOV、MKV 常用视频输入；
- 使用 ffprobe 检查视频、音频和字幕轨道；
- 内嵌软字幕：优先直接导出 SRT，并无重编码移除字幕轨道；
- 硬字幕：支持自动检测字幕区域和手动画框；
- 支持简体中文、繁体中文、英文字幕 OCR；
- 将跨帧 OCR 结果合并成稳定的字幕轨迹；
- 字幕轨迹同时用于生成 SRT 和生成擦除遮罩；
- 提供字幕文字、起止时间和置信度的人工校对界面；
- 提供擦除前抽样预览；
- 默认使用 STTN 修复，提供 LaMa ONNX 备用模式；
- 保留原始音频；
- 支持单文件和文件夹批量处理；
- 支持暂停、取消、失败重试和断点恢复；
- 输出清水视频、SRT、工程数据和质量报告；
- 对输出视频再次检测字幕残留，并标记可疑时间段。

### 2.2 第一版暂不实现

- 自动翻译和字幕配音；
- 手机端和 Web 云端版本；
- 完整视频剪辑、转场、滤镜及模板生产；
- ProPainter 内置与商业分发；
- 强依赖剪映的全自动导出；
- 多人说话人分离；
- 去除复杂移动水印、台标和任意物体。

## 3. 总体处理流程

### 3.1 输入预检

读取分辨率、帧率、时间基、是否可变帧率、视频编码、音频轨道、字幕轨道、时长和旋转信息。

### 3.2 字幕类型分流

- 存在文本软字幕：直接导出或转换成 SRT，清水视频采用无损重新封装；
- 存在图像字幕轨道：OCR 转换成 SRT，清水视频移除字幕轨道后重新封装；
- 不存在字幕轨道但画面有文字：进入硬字幕识别与修复流程；
- 没有检测到字幕：提示用户重新选择区域或确认无需处理。

### 3.3 硬字幕识别

1. 对视频进行场景切换检测和抽帧；
2. 在字幕候选区域执行文字检测；
3. 将文字框扩展为包含描边、阴影的多边形遮罩；
4. 根据位置、文字相似度、帧间隔和置信度建立字幕轨迹；
5. 对同一轨迹内的多帧 OCR 结果进行投票，减少单帧错字；
6. 生成可编辑的字幕条目和 SRT 预览。

### 3.4 字幕校正

默认以画面 OCR 为主。可选启用 faster-whisper，对字幕出现时间段的音频进行识别，用于：

- 提示可能漏掉的对白；
- 辅助修正字幕起止时间；
- 标记 OCR 与对白明显不一致的条目；
- 不直接覆盖翻译字幕、无声字幕和画面说明文字。

### 3.5 视频修复

- STTN：默认模式，用于真人、镜头运动和需要帧间一致性的场景；
- LaMa ONNX：用于静态镜头、动漫、简单纹理或低配置设备；
- 邻帧背景合成：字幕区域在相邻帧暴露时优先使用真实背景；
- 每次场景切换后重置参考帧，防止跨镜头污染；
- 对遮罩边缘进行适量扩张和羽化，覆盖字体描边与阴影。

### 3.6 输出与质检

- 最终视频只进行一次有损编码；
- 原始音频尽量流复制，无法兼容容器时再转码；
- 保留原始时间基，避免可变帧率视频字幕漂移；
- 对修复区域重新执行文字检测；
- 检查音视频时长差、丢帧、黑帧、残留字幕和明显闪烁；
- 将可疑区间写入质量报告，允许从对应时间点重新预览和局部返工。

## 4. 软件模块

### 4.1 桌面界面层

- 文件和文件夹导入；
- 视频播放器与时间轴；
- 字幕区域框选；
- 字幕列表校对；
- 擦除效果对比预览；
- 任务队列和设备设置；
- 日志、错误提示和质量报告。

技术选择：PySide6。界面与推理进程分离，推理崩溃不应导致整个桌面程序退出。

### 4.2 媒体处理层

- ffprobe 媒体分析；
- FFmpeg 解码、抽帧、音频提取、编码和封装；
- CFR/VFR 时间戳换算；
- 临时文件及中间产物管理。

### 4.3 字幕检测与 OCR 层

- RapidOCR/ONNX 作为轻量默认后端候选；
- PaddleOCR 作为增强后端；
- 字幕区域限制、文字框过滤、语言过滤；
- OCR 结果标准化、置信度计算和多帧投票。

### 4.4 字幕轨迹层

- 文字框跨帧关联；
- 字幕出现、消失和淡入淡出判断；
- 相似文本合并及不同字幕拆分；
- SRT 断句、编号、时间冲突修正；
- 人工修订记录。

这是整个系统的核心数据层，同一轨迹必须同时驱动 SRT 与擦除遮罩。

### 4.5 修复引擎层

- 统一的修复引擎接口；
- STTN 后端；
- LaMa ONNX 后端；
- 真实邻帧背景恢复后端；
- 分块、显存控制、失败降级和局部重跑。

### 4.6 任务与工程层

- SQLite 保存任务状态；
- JSON 保存可移植的字幕轨迹、遮罩和处理参数；
- 输入文件指纹，防止工程误套到其他视频；
- 断点、缓存、暂停、恢复和批处理；
- 输出文件原子替换，失败时保留原文件。

### 4.7 可选剪映适配层

第二阶段通过独立适配器生成剪映草稿，将清水视频和校对后的 SRT 放入时间轴。剪映升级导致适配器失效时，不影响核心输出。

## 5. 目录与产物约定

每个任务建立独立工程目录，保存：

- 原视频指纹和媒体信息；
- OCR 原始结果；
- 合并后的字幕轨迹；
- 人工修订记录；
- 遮罩或遮罩生成参数；
- 抽样预览图；
- 处理进度和错误记录；
- 最终 SRT 与质量报告。

临时解码帧和中间视频允许在任务成功后自动清理；工程数据默认保留，以便局部重做。

## 6. 默认运行模式

### 快速模式

用户手动框定固定字幕区域，降低 OCR 频率，使用 STTN 快速修复。适合字幕位置固定、批量视频来源一致的情况。

### 标准模式

自动检测加人工确认，完整 OCR 轨迹、SRT 校对、STTN 修复和输出质检。作为默认模式。

### 低配置模式

降低处理分块和并发，使用 ONNX/CPU 或 LaMa，允许更长处理时间。

## 7. 技术与许可证约束

- 自有代码建议采用明确的商业友好许可证或保持闭源；
- 可参考 GPL 项目的架构思想，但不得复制其受 GPL 约束的实现到闭源产品；
- ProPainter 官方模型仅限非商业用途，第一版不内置；
- 每个 OCR、修复模型和权重必须分别登记来源、版本、许可证和文件哈希；
- FFmpeg 的分发方式和启用编码器需要在打包前进行许可证审核；
- 软件只处理用户拥有编辑权或已获授权的视频，界面中提供用途提示。

## 8. 第一版验收标准

### 功能验收

- 软字幕视频能够正确导出 SRT，并在不重编码视频画面的情况下得到无字幕文件；
- 硬字幕任务能够完成检测、校对、擦除和输出闭环；
- SRT 时间轴与原字幕画面保持同步；
- 输出视频保留音频，分辨率和时长正确；
- 中途中止后能够从已完成阶段恢复；
- OCR或修复失败时给出具体阶段和可执行建议。

### 质量验收

- 建立不少于 30 个授权测试片段，覆盖真人、动漫、静态背景、运动镜头、双行字幕、描边字幕、渐隐字幕和可变帧率视频；
- OCR、时间轴和擦除质量分别评估，不能只以“任务成功”判断；
- 质量门槛最终根据首轮样本基准确定，避免在没有真实素材测试前编造准确率或速度指标；
- 所有低置信度字幕和残留检测结果必须能够定位到具体时间段并人工复核。

## 9. 开发阶段

### 阶段 0：技术验证

用 5 至 10 个真实授权短片验证 FFmpeg 时间轴、OCR轨迹、STTN、LaMa和SRT生成，形成速度、显存、准确率和失败类型报告。

### 阶段 1：命令行处理核心

完成媒体预检、软字幕分流、硬字幕OCR、轨迹、SRT、修复、输出和质量报告。先保证处理核心可测试，再接桌面界面。

### 阶段 2：桌面 MVP

完成导入、框选、字幕校对、预览、任务队列、设置和错误恢复。

### 阶段 3：批处理与稳定性

增加缓存、断点恢复、显存自适应、局部返工、安装包和完整测试样本。

### 阶段 4：剪映适配

在不改变核心处理链路的前提下，增加剪映草稿生成和可选自动导出。

## 10. 开工决策

建议从阶段 0 开始，不直接开发完整界面。阶段 0 通过后，才能锁定默认 OCR 后端、默认修复算法、最低硬件要求和可量化验收门槛。

